---
name: Loader
---

import { Playground, PropsTable } from 'docz'
import { Loader } from '../../src/components'

#Loader

##Properties

<PropsTable of={Loader} />

##Basic usage

<Playground>
  <label
    htmlFor="basic"
    style={{display: 'block', marginTop: 10}}
  >
    basic
  </label>
  <Loader name="basic" />
  <label
    htmlFor="duration"
    style={{display: 'block', marginTop: 10}}
  >
    with specified duration
  </label>
  <Loader name="duration" duration={6} />
  <label
    htmlFor="durationAndProgress"
    style={{display: 'block', marginTop: 10}}
  >
    with specified duration and displayed progress
  </label>
  <Loader name="durationAndProgress" duration={6} progress={true} />
  <label
    htmlFor="size"
    style={{display: 'block', marginTop: 10}}
  >
    with specified diameter in px
  </label>
  <Loader name="size" width={60} duration={6} progress={true} />
</Playground>