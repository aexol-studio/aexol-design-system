---
name: NavBar
---

import { Playground, PropsTable } from 'docz'
import { NavBar } from '../../src/components'

#NavBar

##Properties

<PropsTable of={NavBar} />

##Basic usage

<Playground>
  <NavBar
    color="white"
    buttonText="Sign In"
    onClick={() => {}}
  >
    {[{name: 'workshop', to: '/workshop'}, {name: 'pricing', to: '/pricing'}].map((el, idx) => 
      <a
        href={el.to}
        key={idx}
        style={{marginRight: 50}}
      >
        {el.name}
      </a>
    )}
  </NavBar>
</Playground>